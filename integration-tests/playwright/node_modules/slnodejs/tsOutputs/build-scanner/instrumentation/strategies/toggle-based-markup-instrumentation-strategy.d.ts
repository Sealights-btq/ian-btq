import { MarkupFileInstrumentationStrategy } from "./markup-instrumnetation-strategy";
/**
 * This strategy implements the toggle-based browser instrumentation approach for markup files.
 * It modifies the original file to contain both instrumented and non-instrumented versions of scripts,
 * toggling between them based on the presence of sl-preamble-config.js
 */
export declare class ToggleBasedMarkupInstrumentationStrategy extends MarkupFileInstrumentationStrategy {
    process(inputFile: string, outputFile: string, fileKey: string): Promise<void>;
    private createToggleContent;
}
