import { Logger } from '../contracts';
import { IHttpMetricsStore } from './contracts';
export declare class HttpMetricsStoreFactory {
    private static _instance;
    static create(logger: Logger): IHttpMetricsStore;
}
